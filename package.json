{
  "dependencies": {
    "cannon-es": "^0.20.0",
    "cannon-es-debugger": "^1.0.0",
    "dat.gui": "^0.7.9",
    "three": "^0.150.1"
  },
  "scripts": {
    "start": "parcel ./src/index.html",
    "predeploy": "del dist && parcel build ./src/index.html --public-url https://dicehammer.app/ && copy CNAME ../dist",
    "deploy": "gh-pages -d dist"
  }
}
